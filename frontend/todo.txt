-- correct header responsivness

-- make footer properly



const useStyles = makeStyles(theme => ({
  offset: {
    ...theme.mixins.toolbar,
    flexGrow: 1
  }
}))

function AppBarOffset() {
  const classes = useStyles();
  return <div className={classes.offset} />;
}